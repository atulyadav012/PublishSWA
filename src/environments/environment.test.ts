export const environment = {
  production: false,
  apiUrl: 'http://localhost:21897/api/', // change this URL as TEST API url
  feature: {
    Idle:{
      enable: true,
      idleTimeSeconds: 600,
      timeOutSeconds: 1
    }
  }
};
